[
    {
        "description": "minProperties validation",
        "schema": {"minProperties": 1},
        "tests": [
            {
                "description": "exact length is valid",
                "data": {"0": {}}
            }
        ]
    },
    {
        "description": "minProperties validation with existing object",
        "schema": {"minProperties": 2, "required": ["foo"]},
        "tests": [
            {
                "description": "exact length is valid",
                "data": {"0": {}, "foo": {}}
            }
        ]
    }
]
